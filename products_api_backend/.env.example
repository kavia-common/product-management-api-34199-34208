# Example environment configuration for products_api_backend

# Filesystem path to SQLite database. If not set, defaults to instance/products.db
# DB_PATH=/abs/path/to/products.db

# Informational port variable (the service binds to 0.0.0.0:3001 via run.py)
# PORT=3001

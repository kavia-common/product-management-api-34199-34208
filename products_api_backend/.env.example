# Example environment variables for products_api_backend
# Path to SQLite database file. If unset, defaults to instance/products.db
DB_PATH=/app/data/products.db

# The app always runs on port 3001 via run.py; this variable is informational for orchestration.
PORT=3001
